package com.GoldenOpportunity.Login.enums;

/**
 * System roles that determine authorization levels and capabilities.
 */
public enum Role {
    /** Guest customer with minimal privileges. */
    GUEST,
    /** Front-desk clerk with operational privileges. */
    CLERK,
    /** Administrator with full privileges. */
    ADMIN
}
